name: Android CI

on:
  push:
    branches: [ desa-nampirejo ]
  pull_request:
    branches: [ desa-nampirejo ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: nampirejo47
    - name: desanampirejo07
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build with Gradle
      run: ./gradlew build
